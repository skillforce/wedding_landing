---
interface Props {
  size?: number;
}
const { size = 18 } = Astro.props;
---
<svg width={size} height={Math.round(size * 0.6)} viewBox="0 0 22 13" fill="none">
  <circle cx="8" cy="6.5" r="5.5" stroke="currentColor" stroke-width="1.8"/>
  <circle cx="14" cy="6.5" r="5.5" stroke="currentColor" stroke-width="1.8"/>
</svg>