---
import type { Translations } from "../i18n/translations";
import Button from "./base/Button.astro";
import { ButtonType } from "./base/Button.types";
import IconOrganizer from "./icons/IconOrganizer.astro";
import IconRings from "./icons/IconRings.astro";

interface Props {
  t: Translations;
}
const { t } = Astro.props;
---
<section class="hero">


  <div class="hero-stars">
    <div class="hero-star hero-star-1"></div>
    <div class="hero-star hero-star-2"></div>
    <div class="hero-star hero-star-3"></div>
  </div>

  <p class="hero-eyebrow">{t.hero.eyebrow}</p>
  <div class="hero-logo">
    <img class="hero-logo-light" src="/images/logo/logo_light.avif" alt="Doctor Wedding" />
    <img class="hero-logo-dark"  src="/images/logo/logo_dark.avif"  alt="Doctor Wedding" />
  </div>
  <span class="hero-title-line1 serif">We plan.</span>
  <span class="hero-title-line2 script">You celebrate.</span>
  <p class="hero-sub">{t.hero.subtitle}</p>

  <div class="hero-btns">
    <Button type={ButtonType.Primary}>
      <IconOrganizer />{t.hero.ctaPrimary}
    </Button>
    <Button type={ButtonType.Secondary}>
      <IconRings />{t.hero.ctaSecondary}
    </Button>
  </div>
  <div class="creep-line"></div>
</section>

<style>
  .hero {
    min-height: 100vh; position: relative; overflow: hidden;
    padding: 10px;
    background-image: url("/images/hero/hero_backround_light.avif");
    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
  }
  :global(html.dark) .hero {
    background-image: url("/images/hero/hero_backround_dark.avif");
  }

  .hero-stars { position: absolute; inset: 0; pointer-events: none; overflow: hidden; display: none; }
  :global(html.dark) .hero-stars { display: block; }
  .hero-star { position: absolute; border-radius: 50%; }
  .hero-star-1 { width: 2px; height: 2px; background: rgba(184,148,63,.5); top: 18%; left: 70%; }
  .hero-star-2 { width: 1.5px; height: 1.5px; background: rgba(184,148,63,.4); top: 32%; left: 85%; }
  .hero-star-3 { width: 2px; height: 2px; background: rgba(255,255,255,.25); top: 55%; left: 78%; }

  .hero-eyebrow { font-size: 16px; font-weight: 600; letter-spacing: 3px; text-transform: uppercase; margin-bottom: 20px; color: var(--text-soft); transition: color 0.2s; }

  .hero-logo { margin-bottom: 20px; display: flex; justify-content: center; }
  .hero-logo img { height: 110px; width: auto; }
  .hero-logo-dark { display: none; }
  :global(html.dark) .hero-logo-light { display: none; }
  :global(html.dark) .hero-logo-dark  { display: block; }
  :global(html.dark) .hero-eyebrow { color: var(--gold); opacity: 0.8; }
  .hero-title-line1 { font-size: 57px; font-weight: 700; line-height: 1.1; display: block; color: var(--text-dark); transition: color 0.2s; }
  .hero-title-line2 { font-size: 73px; line-height: 1.1; display: block; margin-bottom: 20px; color: var(--teal-dark); transition: color 0.2s; }
  :global(html.dark) .hero-title-line1 { color: #fff; }
  :global(html.dark) .hero-title-line2 { color: var(--gold-bright); }
  .hero-sub { font-size: 17px; line-height: 1.65; max-width: 380px; margin: 0 auto 28px; color: var(--text-mid); transition: color 0.2s; }
  :global(html.dark) .hero-sub { color: #9ba8c0; }

  .hero-btns { display: flex; gap: 12px; flex-wrap: wrap; justify-content: center; }

  .creep-line { position: absolute; bottom: 0; left: 0; width: 200%; height: 1.5px; background: linear-gradient(90deg, transparent, var(--teal-dark), transparent); animation: creep 4s linear infinite; opacity: 0.4; transition: background 0.2s; }
  :global(html.dark) .creep-line { background: linear-gradient(90deg, transparent, var(--gold), transparent); }

  @media (max-width: 760px) {
    .hero {
      min-height: 100dvh;
      padding: 96px 24px 44px;
      background-image: url("/images/hero/hero_backround_light_mobile.avif");
      background-position: center top;
    }
    :global(html.dark) .hero {
      background-image: url("/images/hero/hero_backround_dark_mobile.avif");
    }
    .hero-logo img { height: 88px; }
    .hero-title-line1 { font-size: 47px; }
    .hero-title-line2 { font-size: 61px; margin-bottom: 16px; }
    .hero-sub { font-size: 16px; max-width: 340px; margin-bottom: 24px; }
  }

  @media (max-width: 480px) {
    .hero {
      min-height: 100dvh;
      padding: 92px 16px 40px;
    }
    .hero-eyebrow { font-size: 15px; letter-spacing: 2px; margin-bottom: 12px; }
    .hero-title-line1 { font-size: 40px; }
    .hero-title-line2 { font-size: 51px; }
    .hero-sub { font-size: 15px; line-height: 1.55; }
    .hero-btns { width: 100%; gap: 10px; }
    .hero-btns :global(.button) { width: 100%; }
  }

  @media (max-width: 340px) {
    .hero-title-line1 { font-size: 37px; }
    .hero-title-line2 { font-size: 47px; }
  }
  @keyframes creep { from { transform: translateX(-50%); } to { transform: translateX(0%); } }
</style>
